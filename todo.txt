
shopping cart old format
<div class="container" style="margin-top:50px;">
    	<div class="row shopping-cart-list list">
            <% for (var i = 0; i < cartItems.length; i++) { %>
                <div class="shopping-cart-item what">
                    <%- cartItems[i].title %><br>
                    <%- cartItems[i].qty %>
                    <i class="fa fa-shopping-cart "></i>
                    <a data-id=<%- cartItems[i].id %> class="hidden-sm delete-item button">Delete item</a>
                    
                </div>
            <% } %>
            
    	</div>
</div>



 if(!(itemId in user.local.cart)){
                    user.local.cart.push(itemId);
                } else{
                    items[itemId].qty = items[itemId].qty +1;
                    items[itemId].price = items[itemId].qty * items[itemId].price;
                }
                
    we are working on the user model, however the else statement works on the item model, if there are 2 users ,
    items model will be confused, dirty data.